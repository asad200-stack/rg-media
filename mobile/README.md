# RG Media Mobile (Flutter)

Native iOS/Android apps — Phase 4.

The web PWA at http://localhost:3000 provides full mobile functionality today.

## Planned

- Flutter app with offline SQLite sync
- Push notifications (FCM/APNs)
- Feature parity with web

## API

All endpoints: `http://localhost:3001/api/v1`  
Auth: `POST /auth/login` → Bearer token
